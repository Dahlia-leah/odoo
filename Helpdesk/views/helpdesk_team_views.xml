<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Tree View for Helpdesk Team -->
        <record id="view_helpdesk_team_tree" model="ir.ui.view">
            <field name="name">helpdesk.team.tree</field>
            <field name="model">helpdesk.team</field>
            <field name="arch" type="xml">
                <tree string="Helpdesk Teams">
                    <field name="name"/>
                </tree>
            </field>
        </record>

        <!-- Form View for Helpdesk Team -->
        <record id="view_helpdesk_team_form" model="ir.ui.view">
            <field name="name">helpdesk.team.form</field>
            <field name="model">helpdesk.team</field>
            <field name="arch" type="xml">
                <form string="Helpdesk Team">
                    <sheet>
                        <group>
                            <field name="name"/>
                        </group>
                        <notebook>
                            <page string="Team Members">
                                <field name="member_ids" widget="many2many_tags"/>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action for Helpdesk Team -->
        <record id="action_helpdesk_team" model="ir.actions.act_window">
            <field name="name">Helpdesk Teams</field>
            <field name="res_model">helpdesk.team</field>
            <field name="view_mode">tree,form</field>
        </record>

        <!-- Menu Item for Helpdesk Team -->
        <menuitem id="menu_helpdesk_team"
                  name="Teams"
                  parent="helpdesk.helpdesk_menu_config"
                  action="action_helpdesk_team"
                  sequence="10"/>
    </data>
</odoo>

